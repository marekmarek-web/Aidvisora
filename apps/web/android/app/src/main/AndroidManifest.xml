<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- legacy scheme -->
                <data android:scheme="aidvisor" />
                <!-- aidvisora://auth/callback (legacy) -->
                <data android:scheme="aidvisora" android:host="auth" android:pathPrefix="/callback" />
                <!-- aidvisora://auth/done – used by /auth/callback route after code exchange -->
                <data android:scheme="aidvisora" android:host="auth" android:path="/done" />
            </intent-filter>

            <!--
                Delta A12 — Android App Links (https://aidvisora.cz/portal/..., /client/..., /auth/...).
                autoVerify=true → Play Protect ověří assetlinks.json na startu a po update.
                AASA ekvivalent: apps/web/src/app/api/assetlinks/route.ts
                (proxováno na /.well-known/assetlinks.json přes next.config.js rewrite).
            -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="aidvisora.cz" android:pathPrefix="/portal" />
                <data android:scheme="https" android:host="aidvisora.cz" android:pathPrefix="/client" />
                <data android:scheme="https" android:host="aidvisora.cz" android:pathPrefix="/auth" />
                <data android:scheme="https" android:host="www.aidvisora.cz" android:pathPrefix="/portal" />
                <data android:scheme="https" android:host="www.aidvisora.cz" android:pathPrefix="/client" />
                <data android:scheme="https" android:host="www.aidvisora.cz" android:pathPrefix="/auth" />
            </intent-filter>
        </activity>

        <activity
            android:name="app.supernotes.sendIntent.SendIntentActivity"
            android:label="@string/app_name"
            android:exported="true"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/pdf" />
                <data android:mimeType="image/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/pdf" />
                <data android:mimeType="image/*" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
</manifest>
