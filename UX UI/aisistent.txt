"use client";

import React, { useState } from 'react';
import { 
  Search, Bell, Filter, RefreshCw, UploadCloud, 
  FileText, Sparkles, AlertCircle, CheckCircle2, 
  Clock, ArrowRight, MoreVertical, BrainCircuit,
  X, ShieldAlert, FileCheck, FileWarning
} from 'lucide-react';

// --- MOCK DATA ---
const MOCK_CONTRACTS = [
  {
    id: 'c1',
    fileName: 'Kooperativa_Zivotni_Pojisteni_Novak.pdf',
    client: 'Jan Novák',
    type: 'Životní pojištění',
    institution: 'Kooperativa',
    status: 'review', // 'processing', 'review', 'done', 'error'
    uploadDate: 'Dnes, 10:42',
    aiConfidence: 94,
    insights: [
      { label: 'Pojistná částka', value: '2 000 000 Kč' },
      { label: 'Měsíční pojistné', value: '1 450 Kč' },
      { label: 'Obmyšlená osoba', value: 'Chybí', alert: true }
    ]
  },
  {
    id: 'c2',
    fileName: 'Hypoteka_CS_Smlouva_Dvorak.pdf',
    client: 'Martin Dvořák',
    type: 'Hypoteční úvěr',
    institution: 'Česká spořitelna',
    status: 'processing',
    uploadDate: 'Dnes, 14:15',
    aiConfidence: null,
    insights: []
  },
  {
    id: 'c3',
    fileName: 'Allianz_Auto_Havarijni_SPZ_1AB2345.pdf',
    client: 'Kristýna Benešová',
    type: 'Pojištění vozidel',
    institution: 'Allianz',
    status: 'done',
    uploadDate: 'Včera',
    aiConfidence: 99,
    insights: [
      { label: 'SPZ', value: '1AB 2345' },
      { label: 'Spoluúčast', value: '5 %, min. 5 000 Kč' }
    ]
  }
];

export default function AIContractReviewView() {
  const [searchQuery, setSearchQuery] = useState('');
  const [showError, setShowError] = useState(true); // Simulace chyby z vašeho screenshotu
  const [isRefreshing, setIsRefreshing] = useState(false);

  const handleRefresh = () => {
    setIsRefreshing(true);
    setShowError(false); // Při obnovení schováme chybu
    setTimeout(() => setIsRefreshing(false), 1000);
  };

  return (
    <div className="min-h-screen bg-[#f8fafc] font-lato text-slate-800 pb-20">
      <style>{`
        @import url('https://fonts.googleapis.com/css2?family=Lato:wght@300;400;700;900&display=swap');
        .font-lato { font-family: 'Lato', sans-serif; }
        .hide-scrollbar::-webkit-scrollbar { display: none; }
        
        /* Pulzující AI border pro nahrávací zónu */
        .ai-dropzone {
          background-image: dashed;
          background-size: 10px 10px;
          transition: all 0.3s ease;
        }
        .ai-dropzone:hover {
          border-color: #8b5cf6;
          background-color: rgba(139, 92, 246, 0.05);
        }
      `}</style>

      {/* --- TOP NAV --- */}
      <header className="bg-white/80 backdrop-blur-md border-b border-slate-100 px-8 py-4 sticky top-0 z-50 flex items-center justify-between shadow-sm">
        <div className="flex items-center gap-10 flex-1">
          <div className="flex items-center gap-3">
            <div className="w-8 h-8 bg-[#1a1c2e] rounded-lg flex items-center justify-center text-white font-black text-sm">W</div>
            <span className="font-black text-lg tracking-tight hidden sm:block">WePlan</span>
          </div>
          <div className="w-px h-5 bg-slate-200"></div>
          <div className="flex items-center gap-2 text-[10px] font-black uppercase tracking-[0.2em] text-slate-400">
             <span>AI Nástroje</span>
             <span className="opacity-30">/</span>
             <span className="text-indigo-600 flex items-center gap-1"><BrainCircuit size={12} /> Review smluv</span>
          </div>
        </div>
        <div className="flex items-center gap-4">
          <button className="p-2 text-slate-400 hover:text-indigo-600 transition-colors"><Bell size={18}/></button>
          <div className="w-8 h-8 rounded-full bg-gradient-to-tr from-indigo-500 to-purple-500 text-white flex items-center justify-center text-xs font-black cursor-pointer shadow-sm">MD</div>
        </div>
      </header>

      <main className="max-w-[1200px] mx-auto p-6 md:p-8 space-y-6">
        
        {/* --- HLAVIČKA ASISTENTA --- */}
        <div className="flex flex-col md:flex-row md:items-end justify-between gap-6 mb-4">
          <div className="flex items-start gap-4">
            <div className="w-14 h-14 rounded-[20px] bg-gradient-to-br from-indigo-500 to-purple-600 flex items-center justify-center text-white shadow-lg shadow-indigo-200 flex-shrink-0">
              <Sparkles size={28} />
            </div>
            <div>
              <h1 className="text-3xl font-black text-slate-900 tracking-tight mb-1">AI Asistent – Review smluv</h1>
              <p className="text-sm font-medium text-slate-500 max-w-xl leading-relaxed">
                Jsem připraven zpracovat vaše smlouvy. Automaticky z nich vyčtu důležitá data, zkontroluji parametry a připravím je k vložení do CRM.
              </p>
            </div>
          </div>
        </div>

        {/* --- UPLOAD ZONE --- */}
        <div className="ai-dropzone border-2 border-dashed border-indigo-200 rounded-[32px] p-10 bg-white text-center cursor-pointer shadow-sm group relative overflow-hidden">
          <div className="absolute top-0 right-0 w-64 h-64 bg-purple-100 opacity-20 rounded-full blur-[80px] pointer-events-none group-hover:opacity-40 transition-opacity"></div>
          
          <div className="w-16 h-16 bg-indigo-50 rounded-2xl flex items-center justify-center text-indigo-500 mx-auto mb-4 group-hover:scale-110 transition-transform">
            <UploadCloud size={32} strokeWidth={1.5} />
          </div>
          <h3 className="text-lg font-black text-slate-800 mb-2">Přetáhněte smlouvy sem</h3>
          <p className="text-sm text-slate-500 font-medium mb-6">nebo klikněte pro výběr souborů (PDF, JPG, PNG). Můžete nahrát i více smluv najednou.</p>
          <button className="px-6 py-2.5 bg-white border border-slate-200 text-slate-700 font-black text-xs uppercase tracking-widest rounded-xl hover:bg-slate-50 hover:border-indigo-200 transition-all shadow-sm">
            Vybrat soubory z počítače
          </button>
        </div>

        {/* --- CHYBOVÁ HLÁŠKA (Dle screenshotu) --- */}
        {showError && (
          <div className="bg-rose-50 border border-rose-200 rounded-2xl p-4 flex items-start justify-between animate-in fade-in slide-in-from-top-2">
            <div className="flex items-center gap-3 text-rose-700">
              <AlertCircle size={20} className="shrink-0" />
              <span className="font-bold text-sm">Načtení seznamu selhalo. Zkontrolujte připojení nebo akci opakujte.</span>
            </div>
            <button onClick={() => setShowError(false)} className="text-rose-400 hover:text-rose-700 transition-colors p-1">
              <X size={16} />
            </button>
          </div>
        )}

        {/* --- OVLÁDACÍ PANEL SEZNAMU --- */}
        <div className="bg-white p-3 rounded-[24px] border border-slate-100 shadow-sm flex flex-col md:flex-row items-center justify-between gap-4">
          
          <div className="relative flex-1 w-full max-w-md focus-within:ring-2 focus-within:ring-indigo-50 rounded-xl transition-all">
            <Search size={18} className="absolute left-4 top-1/2 -translate-y-1/2 text-slate-400" />
            <input 
              type="text" 
              placeholder="Klient, číslo smlouvy, instituce..." 
              value={searchQuery}
              onChange={(e) => setSearchQuery(e.target.value)}
              className="w-full pl-11 pr-4 py-2.5 bg-slate-50/50 border border-slate-200 hover:border-slate-300 rounded-xl text-sm font-medium outline-none focus:border-indigo-400 transition-colors" 
            />
          </div>

          <div className="flex items-center gap-3 w-full md:w-auto overflow-x-auto pb-2 md:pb-0 px-1 md:px-0">
            <div className="relative">
              <select className="pl-10 pr-8 py-2.5 bg-white border border-slate-200 rounded-xl text-sm font-bold text-slate-600 outline-none appearance-none cursor-pointer hover:bg-slate-50 transition-colors whitespace-nowrap">
                <option>Všechny stavy review</option>
                <option>K revizi</option>
                <option>Dokončeno</option>
              </select>
              <Filter size={16} className="absolute left-3.5 top-1/2 -translate-y-1/2 text-slate-400 pointer-events-none" />
            </div>

            <div className="relative">
              <select className="pl-4 pr-8 py-2.5 bg-white border border-slate-200 rounded-xl text-sm font-bold text-slate-600 outline-none appearance-none cursor-pointer hover:bg-slate-50 transition-colors whitespace-nowrap">
                <option>Všechny stavy zpracování</option>
                <option>Zpracovává se</option>
                <option>Aplikováno v CRM</option>
              </select>
            </div>

            <button 
              onClick={handleRefresh}
              className="flex items-center gap-2 px-4 py-2.5 bg-white border border-slate-200 text-slate-700 rounded-xl text-sm font-bold hover:bg-slate-50 transition-colors shadow-sm ml-auto md:ml-0 whitespace-nowrap"
            >
              <RefreshCw size={16} className={isRefreshing ? "animate-spin text-indigo-600" : ""} /> 
              Obnovit
            </button>
          </div>
        </div>

        {/* --- SEZNAM SMLUV --- */}
        <div className="bg-white rounded-[32px] border border-slate-100 shadow-sm overflow-hidden">
          
          <div className="px-6 py-5 border-b border-slate-50/80 bg-slate-50/30 flex items-center justify-between">
            <h2 className="font-black text-slate-800 text-sm flex items-center gap-2">
              Položky <span className="bg-slate-200 text-slate-600 px-2 py-0.5 rounded-md text-xs">{MOCK_CONTRACTS.length}</span>
            </h2>
          </div>

          <div className="flex flex-col">
            {MOCK_CONTRACTS.map((contract) => {
              
              // Stylování podle stavu
              let statusConfig = {};
              switch(contract.status) {
                case 'processing': 
                  statusConfig = { icon: <RefreshCw size={16} className="animate-spin" />, text: 'Zpracovává se', color: 'text-blue-600 bg-blue-50 border-blue-200', dot: 'bg-blue-500 animate-pulse' }; break;
                case 'review': 
                  statusConfig = { icon: <ShieldAlert size={16} />, text: 'Vyžaduje revizi', color: 'text-amber-700 bg-amber-50 border-amber-200', dot: 'bg-amber-500' }; break;
                case 'done': 
                  statusConfig = { icon: <CheckCircle2 size={16} />, text: 'Aplikováno v CRM', color: 'text-emerald-700 bg-emerald-50 border-emerald-200', dot: 'bg-emerald-500' }; break;
                default: 
                  statusConfig = { icon: <FileWarning size={16} />, text: 'Chyba čtení', color: 'text-rose-700 bg-rose-50 border-rose-200', dot: 'bg-rose-500' };
              }

              return (
                <div key={contract.id} className="p-6 border-b border-slate-100 last:border-0 hover:bg-slate-50/50 transition-colors group flex flex-col xl:flex-row xl:items-start gap-6">
                  
                  {/* Ikona a Základní Info */}
                  <div className="flex items-start gap-4 xl:w-[40%]">
                    <div className={`w-12 h-12 rounded-xl flex items-center justify-center flex-shrink-0 border shadow-sm
                      ${contract.status === 'done' ? 'bg-emerald-50 border-emerald-100 text-emerald-600' : 'bg-white border-slate-200 text-indigo-500'}
                    `}>
                      {contract.status === 'done' ? <FileCheck size={24} /> : <FileText size={24} />}
                    </div>
                    <div className="min-w-0 flex-1">
                      <h3 className="font-bold text-slate-900 text-[15px] truncate mb-1 group-hover:text-indigo-600 transition-colors cursor-pointer">
                        {contract.fileName}
                      </h3>
                      <div className="flex flex-wrap items-center gap-2 text-xs font-bold text-slate-500">
                        <span className="text-slate-700">{contract.client}</span>
                        <span className="w-1 h-1 bg-slate-300 rounded-full"></span>
                        <span>{contract.institution}</span>
                        <span className="w-1 h-1 bg-slate-300 rounded-full"></span>
                        <span className="text-slate-400 font-medium">{contract.uploadDate}</span>
                      </div>
                    </div>
                  </div>

                  {/* AI Extrakce dat (Insights) */}
                  <div className="flex-1 xl:px-6 xl:border-l border-slate-100">
                    {contract.status === 'processing' ? (
                      <div className="flex items-center gap-3 text-sm font-medium text-slate-400 h-full">
                        <div className="flex gap-1">
                          <span className="w-2 h-2 bg-blue-400 rounded-full animate-bounce" style={{ animationDelay: '0ms' }}></span>
                          <span className="w-2 h-2 bg-blue-400 rounded-full animate-bounce" style={{ animationDelay: '150ms' }}></span>
                          <span className="w-2 h-2 bg-blue-400 rounded-full animate-bounce" style={{ animationDelay: '300ms' }}></span>
                        </div>
                        AI čte dokument a extrahuje data...
                      </div>
                    ) : (
                      <div className="space-y-3">
                        <div className="flex items-center gap-2 mb-2">
                          <span className="text-[10px] font-black uppercase tracking-widest text-indigo-400 flex items-center gap-1">
                            <Sparkles size={12} /> Extrahovaná data
                          </span>
                          {contract.aiConfidence && (
                            <span className="text-[10px] font-bold bg-indigo-50 text-indigo-700 px-2 py-0.5 rounded-md">
                              Jistota {contract.aiConfidence}%
                            </span>
                          )}
                        </div>
                        
                        <div className="flex flex-wrap gap-2">
                          {contract.insights.map((insight, idx) => (
                            <div key={idx} className={`px-3 py-1.5 rounded-lg text-xs font-bold border flex items-center gap-2
                              ${insight.alert ? 'bg-amber-50 border-amber-200 text-amber-800' : 'bg-white border-slate-200 text-slate-700'}
                            `}>
                              <span className="text-slate-400 font-medium">{insight.label}:</span> 
                              <span>{insight.value}</span>
                              {insight.alert && <AlertCircle size={14} className="text-amber-500" />}
                            </div>
                          ))}
                        </div>
                      </div>
                    )}
                  </div>

                  {/* Status a Akce */}
                  <div className="flex xl:flex-col items-center xl:items-end justify-between xl:w-[200px] gap-4">
                    <div className={`inline-flex items-center gap-1.5 px-3 py-1.5 rounded-xl text-xs font-bold border shadow-sm ${statusConfig.color}`}>
                      <span className={`w-2 h-2 rounded-full ${statusConfig.dot}`}></span>
                      {statusConfig.text}
                    </div>
                    
                    {contract.status === 'review' && (
                      <button className="flex items-center gap-2 px-4 py-2 bg-[#1a1c2e] text-white rounded-xl text-xs font-black uppercase tracking-widest shadow-md hover:bg-[#2a2d4a] transition-all active:scale-95">
                        Provést revizi <ArrowRight size={14} />
                      </button>
                    )}
                    {contract.status === 'done' && (
                      <button className="flex items-center gap-2 px-4 py-2 bg-white border border-slate-200 text-slate-700 rounded-xl text-xs font-black uppercase tracking-widest hover:bg-slate-50 transition-all shadow-sm">
                        Otevřít v CRM <ArrowRight size={14} />
                      </button>
                    )}
                  </div>

                </div>
              );
            })}

            {/* PŘÍKLAD EMPTY STATE (Schováno, protože máme mock data. Dle zadání tam mělo být, tak ho nechám zakomentované pro kontext) */}
            {/* <div className="p-16 flex flex-col items-center justify-center text-center">
              <div className="w-16 h-16 bg-slate-50 rounded-2xl flex items-center justify-center text-slate-300 mb-4">
                <FileText size={32} />
              </div>
              <p className="text-slate-500 font-medium">Žádné položky. Nahrajte smlouvu přes upload.</p>
            </div> 
            */}

          </div>
        </div>

      </main>
    </div>
  );
}