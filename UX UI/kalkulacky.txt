"use client";

import React, { useState } from 'react';
import { 
  Calculator, Search, Plus, Bell, TrendingUp, 
  Home, HeartPulse, PiggyBank, ArrowRight,
  Sparkles, FileText, ChevronRight, BarChart2
} from 'lucide-react';

// --- DATA PRO KALKULAČKY ---
const CALCULATORS = [
  {
    id: 'investicni',
    title: 'Investiční kalkulačka',
    description: 'Projekce zhodnocení – výpočet hodnoty investice v čase podle zvolené strategie.',
    icon: TrendingUp,
    color: 'bg-indigo-600',
    lightBg: 'bg-indigo-50',
    hoverRing: 'group-hover:ring-indigo-100',
    link: '/kalkulacky/investicni',
    tag: 'Nejpoužívanější',
    tagColor: 'text-indigo-700 bg-indigo-100'
  },
  {
    id: 'hypotecni',
    title: 'Hypoteční kalkulačka',
    description: 'Měsíční splátka a náklady hypotéky, srovnání úrokových sazeb bankovního trhu.',
    icon: Calculator,
    color: 'bg-blue-600',
    lightBg: 'bg-blue-50',
    hoverRing: 'group-hover:ring-blue-100',
    link: '/kalkulacky/hypotecni'
  },
  {
    id: 'penzijni',
    title: 'Penzijní kalkulačka',
    description: 'Výpočet státní penze, optimalizace příspěvků a odhad nutných vlastních úspor.',
    icon: PiggyBank,
    color: 'bg-emerald-600',
    lightBg: 'bg-emerald-50',
    hoverRing: 'group-hover:ring-emerald-100',
    link: '/kalkulacky/penzijni'
  },
  {
    id: 'zivotni',
    title: 'Kalkulačka životního pojištění',
    description: 'Analýza rizik a propočet potřebného krytí pro zajištění rodiny a výpadku příjmů.',
    icon: HeartPulse,
    color: 'bg-rose-600',
    lightBg: 'bg-rose-50',
    hoverRing: 'group-hover:ring-rose-100',
    link: '/kalkulacky/zivotni'
  },
];

// --- POMOCNÉ KARTY ---
const RECENT_ANALYSES = [
  { id: 1, client: 'Rodina Novákova', type: 'Hypotéka 4.5M', date: 'Před 2 hodinami' },
  { id: 2, client: 'Ing. Lucie Opalenská', type: 'Investiční plán', date: 'Včera' },
  { id: 3, client: 'Petr Malý', type: 'Životní pojištění', date: '10. března 2026' }
];

export default function CalculatorsHub() {
  const [searchQuery, setSearchQuery] = useState('');

  return (
    <div className="min-h-screen bg-[#f8fafc] font-lato text-slate-800 pb-20">
      <style>{`
        @import url('https://fonts.googleapis.com/css2?family=Lato:wght@300;400;700;900&display=swap');
        .font-lato { font-family: 'Lato', sans-serif; }
        
        /* Glass effect background */
        .hub-bg {
          background-image: 
            radial-gradient(at 0% 0%, rgba(99, 102, 241, 0.05) 0px, transparent 50%),
            radial-gradient(at 100% 0%, rgba(59, 130, 246, 0.05) 0px, transparent 50%);
        }
      `}</style>

      {/* --- TOP NAV (Aidvisora Standard) --- */}
      <header className="bg-white/80 backdrop-blur-md border-b border-slate-100 px-8 py-4 sticky top-0 z-50 flex items-center justify-between shadow-sm">
        <div className="flex items-center gap-10 flex-1">
          <div className="flex items-center gap-3">
            <span className="font-black text-xl text-[#5b2b90] tracking-tight">Aidvisora</span>
          </div>
          <div className="relative group max-w-xl w-full hidden md:block">
            <div className="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none">
              <Search size={16} className="text-slate-400 group-focus-within:text-indigo-500 transition-colors" />
            </div>
            <input 
              type="text" 
              placeholder="Hledat kontakty, smlouvy, případy..." 
              className="w-full pl-11 pr-4 py-2.5 bg-slate-50 border border-slate-200 rounded-xl text-sm font-medium outline-none focus:bg-white focus:ring-2 focus:ring-indigo-100 focus:border-indigo-400 transition-all text-slate-700"
            />
          </div>
        </div>
        <div className="flex items-center gap-4">
          <button className="flex items-center gap-2 px-4 py-2.5 bg-white border border-slate-200 text-slate-700 rounded-xl text-xs font-black uppercase tracking-widest shadow-sm hover:bg-slate-50 transition-all active:scale-95">
            <Plus size={16} /> Nový
          </button>
          <button className="p-2 text-slate-400 hover:text-indigo-600 transition-colors"><Bell size={18}/></button>
          <div className="w-9 h-9 rounded-full bg-indigo-100 text-indigo-700 flex items-center justify-center text-sm font-black shadow-sm cursor-pointer border border-indigo-200">MD</div>
        </div>
      </header>

      <main className="max-w-[1200px] mx-auto p-6 md:p-8 hub-bg min-h-[calc(100vh-73px)]">
        
        {/* --- HLAVIČKA STRÁNKY --- */}
        <div className="flex flex-col md:flex-row md:items-end justify-between gap-6 mb-12">
          <div>
            <div className="flex items-center gap-3 mb-2">
              <h1 className="text-3xl md:text-4xl font-black text-slate-900 tracking-tight">
                Kalkulačky
              </h1>
              <span className="px-3 py-1 bg-slate-100 text-slate-500 text-xs font-black rounded-lg border border-slate-200">
                {CALCULATORS.length} celkem
              </span>
            </div>
            <p className="text-sm font-medium text-slate-500">
              Hypoteční, investiční a další expertní kalkulačky pro přípravu řešení.
            </p>
          </div>
        </div>

        {/* --- GRID KALKULAČEK --- */}
        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 mb-12">
          {CALCULATORS.map((calc, index) => {
            const Icon = calc.icon;
            
            return (
              <a 
                key={calc.id}
                href={calc.link}
                className={`group block bg-white rounded-[32px] p-8 border border-slate-100 shadow-sm hover:shadow-xl hover:border-indigo-100 transition-all duration-300 relative overflow-hidden transform hover:-translate-y-1`}
              >
                {/* Dekorativní pozadí na hover */}
                <div className={`absolute -top-24 -right-24 w-48 h-48 rounded-full blur-[60px] opacity-0 group-hover:opacity-40 transition-opacity duration-500 ${calc.color}`}></div>

                {calc.tag && (
                  <div className={`absolute top-6 right-6 px-3 py-1 rounded-full text-[10px] font-black uppercase tracking-widest ${calc.tagColor}`}>
                    {calc.tag}
                  </div>
                )}

                <div className="flex flex-col h-full relative z-10 text-center items-center justify-center">
                  <div className={`w-16 h-16 rounded-2xl flex items-center justify-center text-white shadow-lg mb-6 transition-transform duration-300 group-hover:scale-110 ring-4 ring-transparent ${calc.hoverRing} ${calc.color}`}>
                    <Icon size={28} strokeWidth={2} />
                  </div>
                  
                  <h2 className="text-lg font-black text-slate-900 mb-3 group-hover:text-indigo-600 transition-colors">
                    {calc.title}
                  </h2>
                  
                  <p className="text-sm font-medium text-slate-500 leading-relaxed max-w-[280px]">
                    {calc.description}
                  </p>
                </div>
              </a>
            );
          })}

          {/* Karta pro vytvoření nové komplexní analýzy (Call to Action) */}
          <div className="md:col-span-2 lg:col-span-1 bg-gradient-to-br from-[#1a1c2e] to-indigo-950 rounded-[32px] p-8 text-white shadow-xl relative overflow-hidden flex flex-col justify-center items-center text-center group cursor-pointer">
            <Sparkles className="absolute -top-4 -right-4 w-32 h-32 text-indigo-500/20 group-hover:rotate-12 transition-transform duration-700" />
            
            <div className="w-16 h-16 rounded-2xl bg-white/10 flex items-center justify-center text-indigo-300 mb-6 backdrop-blur-sm border border-white/10 group-hover:scale-110 transition-transform duration-300">
              <BarChart2 size={28} strokeWidth={2} />
            </div>

            <h2 className="text-lg font-black mb-3 text-white">Komplexní analýza</h2>
            <p className="text-sm font-medium text-indigo-200/80 mb-8 max-w-[280px] leading-relaxed">
              Spojte všechny výpočty do jednoho uceleného finančního plánu pro klienta.
            </p>

            <button className="px-6 py-3 bg-indigo-500 hover:bg-indigo-400 text-white rounded-xl text-sm font-black uppercase tracking-widest transition-colors flex items-center gap-2 shadow-lg shadow-indigo-500/30">
              Vytvořit plán <ArrowRight size={16} />
            </button>
          </div>

        </div>

        {/* --- NEDÁVNÁ AKTIVITA --- */}
        <div className="bg-white rounded-[32px] border border-slate-100 shadow-sm p-8 mt-8">
          <div className="flex items-center justify-between mb-6">
            <h3 className="text-lg font-black text-slate-900 flex items-center gap-2">
              <FileText size={18} className="text-indigo-500" /> Nedávné propočty
            </h3>
            <button className="text-sm font-bold text-indigo-600 hover:text-indigo-800 transition-colors flex items-center gap-1">
              Zobrazit všechny <ChevronRight size={16} />
            </button>
          </div>

          <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
            {RECENT_ANALYSES.map(item => (
              <div key={item.id} className="p-4 rounded-2xl border border-slate-100 bg-slate-50 hover:bg-white hover:shadow-md hover:border-indigo-200 transition-all cursor-pointer group flex items-start gap-4">
                <div className="p-2 bg-white rounded-xl shadow-sm border border-slate-100 text-indigo-500 group-hover:bg-indigo-50 group-hover:text-indigo-600 transition-colors">
                  <FileText size={20} />
                </div>
                <div>
                  <h4 className="font-bold text-sm text-slate-900 group-hover:text-indigo-600 transition-colors mb-0.5">{item.client}</h4>
                  <p className="text-xs font-bold text-slate-500 mb-2">{item.type}</p>
                  <span className="text-[10px] font-black uppercase tracking-widest text-slate-400">{item.date}</span>
                </div>
              </div>
            ))}
          </div>
        </div>

      </main>

      {/* --- PLOVOUCÍ AI ASISTENT --- */}
      <div className="fixed bottom-8 right-8 z-[100]">
        <button className="group relative p-[2.5px] rounded-full bg-gradient-to-r from-[#3b82f6] via-[#a855f7] to-[#ec4899] shadow-lg hover:shadow-xl hover:-translate-y-1 transition-all duration-300">
          <div className="bg-white rounded-full p-3 flex items-center justify-center text-[#5b2b90] group-hover:bg-slate-50 transition-colors">
            <Sparkles size={24} />
          </div>
        </button>
      </div>

    </div>
  );
}